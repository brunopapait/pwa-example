import React from 'react';
import Template from '../Template';

export default function CurrentTasks({ }) {
    return (
        <>
            <Template title="Tarefas atuais" status="Atuais" />
        </>
    );
}